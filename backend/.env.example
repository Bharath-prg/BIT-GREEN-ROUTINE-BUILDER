# ===== AUTHENTICATION SYSTEM CONFIG =====
# Complete .env template for Green Routine Builder Backend
# Copy this file to .env and update with your actual values

# ===== SERVER CONFIGURATION =====
PORT=5000
NODE_ENV=development

# ===== DATABASE (MongoDB Atlas) =====
# Get this from MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/green-routine-builder?retryWrites=true&w=majority

# ===== JWT (JSON Web Tokens) =====
# Generate secure secrets using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Access token secret (used for short-lived tokens - 1 hour)
JWT_SECRET=your-super-secret-jwt-access-key-min-32-characters-change-in-production

# Refresh token secret (used for long-lived tokens - 7 days)
JWT_REFRESH_SECRET=your-super-secret-jwt-refresh-key-min-32-characters-change-in-production

# Token expiry times
JWT_EXPIRE=1h
JWT_REFRESH_EXPIRE=7d

# ===== EMAIL CONFIGURATION (Gmail with App Password) =====
# 1. Enable 2-Factor Authentication on Gmail
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select Mail and Other (Windows, Mac, Linux)
# 4. Generate app password (16 characters)
# 5. Copy password below (no spaces)

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-char-app-password
EMAIL_FROM=Green Routine Builder <your-email@gmail.com>

# ===== FRONTEND CONFIGURATION =====
# URL of the frontend application (for CORS)
CLIENT_URL=http://localhost:5173

# ===== OPTIONAL: REFRESH TOKEN STORAGE =====
# If using Redis for refresh token blacklist (optional)
REDIS_URL=redis://localhost:6379
